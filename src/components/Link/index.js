import styled from "styled-components";

const Link = styled.a`
  background-image: var(--colorLinks);
  text-decoration: none;
  -webkit-text-fill-color: transparent;
  -webkit-background-clip: text;
`;

export default Link;
